# Exclude the local cache folder from source distributions and built packages
recursive-exclude cache *

# Common extras you may want to exclude from builds
global-exclude __pycache__
global-exclude *.py[cod]